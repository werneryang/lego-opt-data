# stock-data

Minimal pipeline scaffolding for stock data collection (daily bars, volatility, fundamentals).

## Setup (monorepo)
- From repo root: `python3.11 -m venv .venv && source .venv/bin/activate && make install`
- Or from this folder: `pip install -r ../../requirements-dev.lock && pip install -e . --no-deps`
