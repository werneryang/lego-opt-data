# 2025-12-13 阶段性收尾：文档与范围对齐

## 背景
生产运行已覆盖 `config/universe.csv` 全量清单（超出原 Stage 2/Top 10 目标），阶段性目标从“扩容推进”转为“稳定运行 + 可交接”。为减少值班成本与信息不一致，本次集中梳理并对齐文档入口、生产命令与范围说明。

## 主要改动
- **生产运维入口收敛**：`docs/ops/ops-runbook.md` 顶部增加“一屏清单”（明日值班必跑命令 + UI + 验收命令），并将非生产内容（诊断/路线图/长篇背景）移出。
- **文档索引补齐**：新增 `docs/README.md` 作为文档目录，明确 Production / QA-Test / Dev / Contracts&ADR / Legacy 分工。
- **阶段性总结**：新增 `docs/ops/project-summary.md`，记录当前范围、调度链路、关键命令、验收标准与已知风险/依赖，便于交接。
- **范围说明对齐**：更新 `SCOPE.md` 的日终/次日任务时间（17:30 close-snapshot→rollup、次日 04:30 enrichment）、当前并发与 best practices 入口引用。
- **计划与 TODO 同步**：在 `PLAN.md` 与 `TODO.now.md` 记录“全量 universe 已覆盖、Stage 3 暂缓”的结论，并保持与 `.agent` 摘要一致。

## 后续事项（可选）
- 若需要进一步降低运维成本：补齐 launchd/systemd 示例模板与配置校验脚本（保证 `TZ`、`IB_*`、依赖与路径一致）。
- 若要恢复 Stage 3（更高并发/分批调度）：先基于 `state/run_logs/metrics/` 与 pacing 告警做评审，再推进限速/并发调整。

