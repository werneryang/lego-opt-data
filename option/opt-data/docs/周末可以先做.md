周末可以先做不依赖实时市场的工作，避免被缺盘数据卡住，建议：

配置/文档梳理：补充 ops-runbook/SCOPE 里盘中/收盘双清单、reqtickers 默认值等近期改动；整理 compare 测试流程。
离线数据校验：对本地 close/intraday 分区做完整性检查（主键重复、缺字段、行数对齐），生成 QA 报告。
回放/模拟：用 --simulate 跑一日闭环（测试配置），确认 rollup/enrichment 流程在无行情情况下的健壮性。
缓存/脚本清理：检查 state_test_compare、data_test_compare 等测试产物是否需要归档或清理。
计划和 ADR：如有合约发现/参考价重试策略需要正式化，起草 ADR 或 TODO。
这些都可以在不依赖交易日行情的情况下完成。