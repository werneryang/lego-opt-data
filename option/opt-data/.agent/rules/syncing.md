# 文档同步规则

- 更新项目文档（如 `docs/`, `SCOPE.md`, `PLAN.md`, `TODO.now.md` 等）时，应同步更新 `.agent/` 下的对应规则或摘要，确保两处信息一致。
- 更新 `.agent/` 下的规则/指南时，应在相关正式文档中反映同样的变更（位置参照原主题：运维到 `docs/ops-runbook.md`，范围到 `SCOPE.md`，计划到 `PLAN.md/TODO.now.md` 等）。
- 若暂无法同步，需在变更描述中标记待办并尽快完成对侧更新，避免出现冲突指引。
